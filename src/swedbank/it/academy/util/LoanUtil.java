package swedbank.it.academy.util;

import swedbank.it.academy.domain.Loan;
import swedbank.it.academy.domain.VehicleLoan;

import java.math.BigDecimal;

public class LoanUtil {
    BigDecimal vehicleDepreciation = BigDecimal.valueOf(0);

    /*
    public static void calculateVehicleDepreciation(){

        vehicleDepreciation = initialValue * yearsInUse / maximumYears;
        yearsInUse = diffInYears(currentYear - manufacturedYear);
    }
    */

}
